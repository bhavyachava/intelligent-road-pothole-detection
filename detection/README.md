# Pothole Detection

This module handles pothole detection from road images or video frames using the trained object detection model.

The detection output will include the identified pothole location within the image and the corresponding confidence score.
